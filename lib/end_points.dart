
const  login = 'login';

const  register = 'register';

const  verifyLoginOTP = 'verify-otp';

const  verifyRegisterOTP = 'verify-register-otp';

const logout = 'logout';

const sliders = 'sliders';

const products = 'products';

const categories = 'categories';

const getProfile = 'user/profile';

const productsLimit = 'products?limit=50' ;

const cart = 'cart';

const getFavorites = 'user/favorites';

const updateProfile = 'user/profile';

const pages = 'pages';

const contactUs = 'contact-us';

const filterPrice = 'products?filter[price]=-1111,10000';

const notifications = 'notifications';

const userAddresses = 'user/addresses';

const orders = 'orders';

const countries = 'countries';

const filterProduct = 'products?sort=total_orders';

const deleteaccount = 'delete-account';

const applyCoupon = 'apply-coupon';








